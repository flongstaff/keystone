---
phase: 16-global-deployment-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/toolkit-discovery.sh
  - ~/.claude/skills/wizard-detect.sh
  - ~/.claude/skills/wizard.md
  - ~/.claude/skills/wizard-backing-agent.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 4 v1.1 skill files exist at ~/.claude/skills/ and are byte-for-byte identical to project-local copies"
    - "toolkit-discovery.sh and wizard-detect.sh have executable permissions at global path"
    - "Running wizard-detect.sh from a non-Keystone directory finds toolkit-discovery.sh via SCRIPT_DIR and produces a wizard-state.json with non-zero toolkit discovery counts"
    - "toolkit-registry.json is gitignored and does not appear in git status"
  artifacts:
    - path: "~/.claude/skills/toolkit-discovery.sh"
      provides: "Global toolkit discovery scanner (new file)"
    - path: "~/.claude/skills/wizard-detect.sh"
      provides: "Global wizard detection with v1.1 toolkit integration"
    - path: "~/.claude/skills/wizard.md"
      provides: "Global wizard skill with Step 2.5 injection and dynamic catalog"
    - path: "~/.claude/skills/wizard-backing-agent.md"
      provides: "Global backing agent with Step 2.5 bridge capability block"
  key_links:
    - from: "~/.claude/skills/wizard-detect.sh"
      to: "~/.claude/skills/toolkit-discovery.sh"
      via: "SCRIPT_DIR relative path invocation"
      pattern: 'SCRIPT_DIR.*toolkit-discovery\.sh'
    - from: "~/.claude/skills/wizard.md"
      to: "~/.claude/skills/wizard-detect.sh"
      via: "bash invocation from Step 1"
      pattern: "wizard-detect.sh"
    - from: ".gitignore"
      to: "toolkit-registry.json"
      via: "gitignore entry"
      pattern: "toolkit-registry.json"
---

<objective>
Deploy all 4 v1.1 skill files from project-local `skills/` to `~/.claude/skills/`, verify byte-for-byte sync, confirm cross-project path resolution for the newly deployed toolkit-discovery.sh, and verify toolkit-registry.json is gitignored.

Purpose: Make all v1.1 Dynamic Toolkit Discovery changes live for every project context, not just Keystone. This is the final phase before milestone closure.
Output: 4 globally deployed skill files, verified functional from non-Keystone directory, gitignore confirmed.
</objective>

<execution_context>
@/Users/flong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-global-deployment-sync/16-CONTEXT.md
@.planning/phases/16-global-deployment-sync/16-RESEARCH.md
@.planning/phases/16-global-deployment-sync/16-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy all 4 skill files to global path and verify sync</name>
  <files>~/.claude/skills/toolkit-discovery.sh, ~/.claude/skills/wizard-detect.sh, ~/.claude/skills/wizard.md, ~/.claude/skills/wizard-backing-agent.md</files>
  <action>
Copy all 4 skill files from project-local to global using `cp -p` (preserves permissions, especially executable bit on .sh files). Use absolute paths — local source is `/Users/flong/Developer/keystone/skills/[file]`, global target is `~/.claude/skills/[file]`. Do NOT quote the tilde (quoting suppresses tilde expansion).

Commands (run all 4 copies, then all 4 diffs, then 2 executable checks):

1. Copy all 4 files:
   - `cp -p /Users/flong/Developer/keystone/skills/toolkit-discovery.sh ~/.claude/skills/toolkit-discovery.sh`
   - `cp -p /Users/flong/Developer/keystone/skills/wizard-detect.sh ~/.claude/skills/wizard-detect.sh`
   - `cp -p /Users/flong/Developer/keystone/skills/wizard.md ~/.claude/skills/wizard.md`
   - `cp -p /Users/flong/Developer/keystone/skills/wizard-backing-agent.md ~/.claude/skills/wizard-backing-agent.md`

2. Verify all 4 diffs are zero (each `diff` must exit 0 with no output):
   - `diff /Users/flong/Developer/keystone/skills/toolkit-discovery.sh ~/.claude/skills/toolkit-discovery.sh`
   - `diff /Users/flong/Developer/keystone/skills/wizard-detect.sh ~/.claude/skills/wizard-detect.sh`
   - `diff /Users/flong/Developer/keystone/skills/wizard.md ~/.claude/skills/wizard.md`
   - `diff /Users/flong/Developer/keystone/skills/wizard-backing-agent.md ~/.claude/skills/wizard-backing-agent.md`

3. Verify executable bit on the 2 shell scripts:
   - `test -x ~/.claude/skills/toolkit-discovery.sh`
   - `test -x ~/.claude/skills/wizard-detect.sh`

IMPORTANT: Direction is local -> global. Do NOT reverse the arguments. toolkit-discovery.sh is NEW globally (does not exist yet) — this is the one file that is not an update but a fresh deployment.
  </action>
  <verify>
    <automated>diff /Users/flong/Developer/keystone/skills/toolkit-discovery.sh ~/.claude/skills/toolkit-discovery.sh && diff /Users/flong/Developer/keystone/skills/wizard-detect.sh ~/.claude/skills/wizard-detect.sh && diff /Users/flong/Developer/keystone/skills/wizard.md ~/.claude/skills/wizard.md && diff /Users/flong/Developer/keystone/skills/wizard-backing-agent.md ~/.claude/skills/wizard-backing-agent.md && test -x ~/.claude/skills/toolkit-discovery.sh && test -x ~/.claude/skills/wizard-detect.sh && echo "ALL MATCH"</automated>
  </verify>
  <done>All 4 global skill files are byte-for-byte identical to project-local copies. toolkit-discovery.sh and wizard-detect.sh have executable permission at global path. diff exits 0 for all 4 files.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-project functional test and gitignore verification</name>
  <files>None (verification-only — no files created in project)</files>
  <action>
Two verification steps that confirm v1.1 works outside the Keystone project directory:

**Step 1: Cross-project path resolution test from /tmp**

Run from `/tmp` (not from Keystone directory) to confirm SCRIPT_DIR resolution works:

1. `cd /tmp && mkdir -p .claude`
2. Run toolkit-discovery.sh standalone: `bash ~/.claude/skills/toolkit-discovery.sh 2>/dev/null | python3 -m json.tool | head -20` — confirm valid JSON with non-zero agent count
3. Run wizard-detect.sh: `cd /tmp && bash ~/.claude/skills/wizard-detect.sh 2>/dev/null` — confirm wizard-state.json is created at `/tmp/.claude/wizard-state.json`
4. Extract toolkit counts: `python3 -c "import json; s=json.load(open('/tmp/.claude/wizard-state.json')); print('toolkit counts:', s.get('toolkit', {}).get('counts', 'MISSING'))"` — confirm agents count > 0

This proves wizard-detect.sh finds toolkit-discovery.sh via SCRIPT_DIR when both are at ~/.claude/skills/ and the working directory is NOT Keystone.

**Step 2: Gitignore verification**

From project root (/Users/flong/Developer/keystone):
1. `grep "toolkit-registry.json" /Users/flong/Developer/keystone/.gitignore` — confirm entry exists
2. `git -C /Users/flong/Developer/keystone status --short | grep toolkit` — expect NO output (file is properly ignored)

Cleanup: Leave /tmp/.claude/ artifacts for OS cleanup (non-sensitive discovery outputs, macOS reclaims /tmp on reboot).
  </action>
  <verify>
    <automated>cd /tmp && mkdir -p .claude && bash ~/.claude/skills/toolkit-discovery.sh 2>/dev/null | python3 -m json.tool > /dev/null && bash ~/.claude/skills/wizard-detect.sh 2>/dev/null && python3 -c "import json; s=json.load(open('/tmp/.claude/wizard-state.json')); assert s.get('toolkit', {}).get('counts', {}).get('agents', 0) > 0, 'No agents found'" && grep -q "toolkit-registry.json" /Users/flong/Developer/keystone/.gitignore && echo "FUNCTIONAL TEST PASS"</automated>
  </verify>
  <done>wizard-detect.sh run from /tmp successfully discovers toolkit-discovery.sh via SCRIPT_DIR, produces wizard-state.json with non-zero toolkit agent count. toolkit-registry.json appears in .gitignore and does not appear in git status output.</done>
</task>

</tasks>

<verification>
Full verification suite (AND-chained, must all pass):

```bash
diff /Users/flong/Developer/keystone/skills/toolkit-discovery.sh ~/.claude/skills/toolkit-discovery.sh && \
diff /Users/flong/Developer/keystone/skills/wizard-detect.sh ~/.claude/skills/wizard-detect.sh && \
diff /Users/flong/Developer/keystone/skills/wizard.md ~/.claude/skills/wizard.md && \
diff /Users/flong/Developer/keystone/skills/wizard-backing-agent.md ~/.claude/skills/wizard-backing-agent.md && \
test -x ~/.claude/skills/toolkit-discovery.sh && \
test -x ~/.claude/skills/wizard-detect.sh && \
grep -q "toolkit-registry.json" /Users/flong/Developer/keystone/.gitignore && \
echo "ALL PASS"
```
</verification>

<success_criteria>
1. `~/.claude/skills/toolkit-discovery.sh` exists and matches project-local (diff exits 0)
2. `~/.claude/skills/wizard-detect.sh` matches project-local (diff exits 0)
3. `~/.claude/skills/wizard.md` matches project-local (diff exits 0)
4. `~/.claude/skills/wizard-backing-agent.md` matches project-local (diff exits 0)
5. Both .sh files have executable bit at global path
6. Running wizard-detect.sh from /tmp discovers agents via toolkit-discovery.sh (non-zero count)
7. toolkit-registry.json appears in .gitignore and not in git status

After verification passes, run `/gsd:complete-milestone` to close v1.1.
</success_criteria>

<output>
After completion, create `.planning/phases/16-global-deployment-sync/16-01-SUMMARY.md`
</output>
