---
phase: 15-dynamic-catalog-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/wizard.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03]

must_haves:
  truths:
    - "Selecting 'Discover tools' from any post-status menu displays tools from toolkit-registry.json when the registry is valid"
    - "Tools are grouped by stage (Research/Planning/Execution/Review), with Keystone tools first, then user tools sub-grouped by type"
    - "When toolkit-registry.json is missing or malformed, the hardcoded Phase 7 catalog displays without errors"
    - "All 18 Keystone tools (11 agents + 4 skills + 3 hooks) appear in both dynamic output and hardcoded fallback"
    - "Running 'Discover tools' from any of the 4 menu variants produces the same catalog output"
  artifacts:
    - path: "skills/wizard.md"
      provides: "Shared Display Catalog block with dynamic registry rendering and hardcoded fallback"
      contains: "## Display Catalog"
  key_links:
    - from: "skills/wizard.md Option 4 handlers"
      to: "## Display Catalog section"
      via: "prose redirect: Go to ## Display Catalog"
      pattern: "Go to ## Display Catalog"
    - from: "## Display Catalog"
      to: ".claude/toolkit-registry.json"
      via: "python3 inline JSON parse with fallback"
      pattern: "toolkit-registry.json"
    - from: "## Display Catalog"
      to: "skills/toolkit-discovery.sh"
      via: "bash call before reading registry"
      pattern: "toolkit-discovery.sh"
---

<objective>
Replace the 4 duplicate hardcoded catalog blocks in wizard.md with a single shared "Display Catalog" instruction section that dynamically reads toolkit-registry.json, groups tools by stage then type with Keystone tools first, and falls back to the hardcoded Phase 7 catalog when the registry is absent or malformed.

Purpose: Fulfill CAT-01/02/03 -- make "Discover tools" show the user's actual installed toolkit from the live registry rather than a frozen 11-agent snapshot, while preserving the hardcoded fallback for fresh installs.

Output: Modified skills/wizard.md with ~234 fewer duplicate lines, one shared Display Catalog section, and a Context Budget Discipline exception for Option 4.
</objective>

<execution_context>
@/Users/flong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-dynamic-catalog-display/15-CONTEXT.md
@.planning/phases/15-dynamic-catalog-display/15-RESEARCH.md
@.planning/phases/12-core-discovery-scanner/12-01-SUMMARY.md

<interfaces>
<!-- Key structures the executor needs. Extracted from codebase. -->

From .claude/toolkit-registry.json (schema_version 1):
```json
{
  "schema_version": 1,
  "scanned_at": "2026-03-13T16:44:17Z",
  "counts": { "agents": 176, "skills": 28, "hooks": 24, "mcp": 0 },
  "tools": [
    {
      "name": "some-agent",
      "type": "agent",
      "description": "One-liner from YAML frontmatter",
      "stages": ["execution", "planning"],
      "source": "~/.claude/agents/some-agent.md",
      "model": "sonnet",
      "tools": ["Read", "Bash"]
    }
  ]
}
```
Types: "agent", "skill", "hook", "mcp"
Stages: "research", "planning", "execution", "review" (multi-value array)

From skills/wizard.md current structure:
- 4 Option 4 handlers at lines ~176, ~253, ~335, ~403
- Each inlines ~78 lines of identical catalog text
- Context Budget Discipline at line 664 currently prohibits reading toolkit-registry.json
- wizard-state.json `project_type` used for active domain agent marking (already loaded in Step 2)

CRITICAL -- Parity gap (from 15-RESEARCH.md):
- 4/11 Keystone agents (admin-docs-agent, godot-dev-agent, it-infra-agent, project-setup-advisor) are NOT in toolkit-registry.json
- 4/4 Keystone skills (wizard, wizard-backing-agent, wizard-detect, toolkit-discovery) are NOT in toolkit-registry.json
- All 3 hooks ARE in the registry
- Therefore: Keystone tools section MUST be hardcoded within the shared block, not sourced from registry
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate Option 4 handlers into shared Display Catalog block</name>
  <files>skills/wizard.md</files>
  <action>
Read `skills/wizard.md` fully. Make three coordinated edits:

**Edit A: Replace 4 Option 4 handler bodies with single-line redirects**

Find all 4 Option 4 (Discover tools) handler bodies (~lines 176, 253, 335, 403). Each currently contains ~78 lines of inline catalog text plus active-marking logic. Replace each handler body with a redirect of the form:

```markdown
- **Option 4 (Discover tools):** Go to ## Display Catalog below, then re-present this SAME [menu-name] menu.
```

Where `[menu-name]` is the specific menu context:
- full-stack uat-passing: "uat-passing menu"
- full-stack non-uat-passing: "non-uat-passing menu"
- gsd-only uat-passing: "uat-passing menu"
- gsd-only non-uat-passing: "non-uat-passing menu"

Each redirect MUST include the "re-present" instruction naming the specific menu so Step 4 of Display Catalog knows which menu to return to.

**Edit B: Create the shared "## Display Catalog" section**

Insert a new section before "## Context Budget Discipline" (currently the last section in the file). The section structure:

```markdown
## Display Catalog

This section is invoked by all "Discover tools" Option 4 handlers.

**Step 1: Refresh registry**
Run: `bash skills/toolkit-discovery.sh` (if local file exists) or `bash ~/.claude/skills/toolkit-discovery.sh`. The script handles TTL internally and exits fast when the registry is fresh. Suppress stderr with 2>/dev/null.

**Step 2: Read and render registry**
Read `.claude/toolkit-registry.json`. Parse it as JSON. If the file does not exist or JSON parsing fails, go to Step 3 (Fallback Display).

If valid, read `project_type` from wizard-state.json (already in context from Step 2). Render the dynamic catalog using the following structure:

Summary header line:
**{N}** agents . **{N}** skills . **{N}** hooks . **{N}** MCP servers

(Use counts from registry `counts` object.)

Then for each stage in order [Research, Planning, Execution, Review]:

Print stage heading: `### {Stage}`

**Keystone tools** subsection (hardcoded -- these are NOT reliably in the registry):

Research stage Keystone tools:
- context-health-monitor -- Detects drift between what was planned and what was built
- doc-shard-bridge -- Creates trimmed per-phase context shards from BMAD docs
- stack-update-watcher -- Checks for BMAD/GSD updates and produces an action plan

Planning stage Keystone tools:
- bmad-gsd-orchestrator -- Bridges BMAD planning docs to GSD execution structure
- phase-gate-validator -- Validates a GSD phase is complete before advancing
- project-setup-advisor -- Detects setup state and outputs the exact workflow to follow
- project-setup-wizard -- Detects installed tooling and produces step-by-step workflow

Execution stage Keystone tools:
- bmad-gsd-orchestrator -- Bridges BMAD planning docs to GSD execution structure
- doc-shard-bridge -- Creates trimmed per-phase context shards from BMAD docs

Review stage Keystone tools:
- context-health-monitor -- Detects drift between what was planned and what was built
- phase-gate-validator -- Validates a GSD phase is complete before advancing

Domain agents (shown in every stage where relevant, with active marking):
- admin-docs-agent -- Administrative docs, runbooks, SOPs, internal communications
- godot-dev-agent -- Godot game development: GDScript, scenes, nodes, signals
- it-infra-agent -- IT infrastructure and DevOps with safety enforcement
- open-source-agent -- Open source management, GitHub Actions, Next.js/TypeScript

Apply active-marking: if `project_type` matches a domain agent (docs -> admin-docs-agent, game -> godot-dev-agent, infra -> it-infra-agent, open-source -> open-source-agent), append " (active)" to that entry. If project_type is null or "web", no marking.

Skills (shown once under Planning stage Keystone):
- wizard -- Interactive wizard: detects project state, presents next action
- wizard-backing-agent -- Bridge coordinator: BMAD-to-GSD bridge and traceability display
- wizard-detect -- Shell detection script: computes project state, writes wizard-state.json
- toolkit-discovery -- Scans installed agents, skills, hooks, and MCP servers into registry

Hooks (shown once under Review stage Keystone):
- session-start -- Shows project state, GSD phase, BMAD status, and update banner at session start
- stack-update-banner -- Checks for BMAD/GSD version updates (non-blocking, cached)
- post-write-check -- Checks for safety issues after every file write

**Installed tools** subsection (from registry, filtered to NON-Keystone):

For each stage, filter `tools` array to entries where `stage` is in the tool's `stages` array AND the tool's `name` is not in the KEYSTONE_NAMES set. Group the filtered tools by type:

*Agents ({count})*
Show up to 10 entries in format: `- {name} -- {description}`
If more than 10, add: `... and {remaining} more`

*Skills ({count})*
Show up to 10 entries, same format and cap.

*MCP ({count})*
Show ALL MCP entries (typically few). Format: `- {name} (configured) -- {description}`

If a type sub-group has 0 entries for this stage, skip it entirely.
If a stage has 0 user-installed tools (only Keystone), skip the "Installed tools" subsection for that stage.

KEYSTONE_NAMES set (hardcoded in the instruction block):
```
bmad-gsd-orchestrator, context-health-monitor, doc-shard-bridge,
phase-gate-validator, project-setup-wizard, project-setup-advisor,
stack-update-watcher, admin-docs-agent, godot-dev-agent, it-infra-agent,
open-source-agent, wizard, wizard-backing-agent, wizard-detect,
toolkit-discovery, session-start, stack-update-banner, post-write-check
```

After all 4 stages, print footer:
*Source: toolkit-registry.json (scanned {scanned_at})*

**Step 3: Fallback Display**
If registry was missing or malformed, display the hardcoded catalog:

---

### Agents

#### Entry
- **project-setup-advisor** -- Detects setup state and outputs the exact workflow to follow
- **project-setup-wizard** -- Detects installed tooling and produces step-by-step workflow

#### Bridge
- **bmad-gsd-orchestrator** -- Bridges BMAD planning docs to GSD execution structure
- **context-health-monitor** -- Detects drift between what was planned and what was built
- **doc-shard-bridge** -- Creates trimmed per-phase context shards from BMAD docs
- **phase-gate-validator** -- Validates a GSD phase is complete before advancing

#### Domain
- **admin-docs-agent** -- Administrative docs, runbooks, SOPs, internal communications
- **godot-dev-agent** -- Godot game development: GDScript, scenes, nodes, signals
- **it-infra-agent** -- IT infrastructure and DevOps with safety enforcement
- **open-source-agent** -- Open source management, GitHub Actions, Next.js/TypeScript

#### Maintenance
- **stack-update-watcher** -- Checks for BMAD/GSD updates and produces an action plan

### Skills
- **wizard** -- Interactive wizard: detects project state, presents next action
- **wizard-backing-agent** -- Bridge coordinator: BMAD-to-GSD bridge and traceability display
- **wizard-detect** -- Shell detection script: computes project state, writes wizard-state.json
- **toolkit-discovery** -- Scans installed agents, skills, hooks, and MCP servers into registry

### Hooks
- **session-start** -- Shows project state, GSD phase, BMAD status, and update banner at session start
- **stack-update-banner** -- Checks for BMAD/GSD version updates (non-blocking, cached)
- **post-write-check** -- Checks for safety issues after every file write

---

Apply active-marking for domain agents using same project_type logic.

*Showing built-in catalog. Run toolkit-discovery.sh for full scan.*

**Step 4: Return to calling menu**
Return to the menu that triggered this section and re-present it.

**IMPORTANT formatting notes (per user decisions):**
- Entry format for dynamic catalog: `- name -- one-liner description` (no bold, no activation commands)
- Entry format for fallback catalog: `- **name** -- one-liner description` (bold names preserved for fallback, matches Phase 7 original format; no activation commands)
- MCP entries in dynamic catalog: append `(configured)` after name
- The fallback catalog uses the Phase 7 type-first format as-is -- no stage re-grouping
- The fallback catalog now includes toolkit-discovery as the 4th skill (added Phase 12, was missing from Phase 7 original)

**Edit C: Update Context Budget Discipline**

Find the last line (currently line 664): "The only toolkit data source is wizard-state.json toolkit.by_stage (already loaded in Step 2). Never read toolkit-registry.json from wizard.md."

Replace with: "The only toolkit data source for Steps 1-3 is wizard-state.json toolkit.by_stage (already loaded in Step 2). Exception: ## Display Catalog may run `bash skills/toolkit-discovery.sh` and read `.claude/toolkit-registry.json` -- this is the PERF-03 designated lazy-load point."

Also update the "Do NOT run bash yourself" bullet to: "Do NOT run bash yourself -- bash runs via the detection script (wizard-detect.sh) in Step 1. Exception: ## Display Catalog runs toolkit-discovery.sh for registry refresh."
  </action>
  <verify>
    <automated>HEADING=$(grep -c "^## Display Catalog$" skills/wizard.md); REDIR=$(grep -c "Go to ## Display Catalog" skills/wizard.md); REPRES=$(grep -c "re-present" skills/wizard.md); echo "Heading=$HEADING Redirects=$REDIR RePresent=$REPRES"; [ "$HEADING" -eq 1 ] && [ "$REDIR" -eq 4 ] && [ "$REPRES" -ge 4 ] && grep -q "PERF-03" skills/wizard.md && grep -q "toolkit-discovery" skills/wizard.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- All 4 Option 4 handlers contain single-line redirects to ## Display Catalog
- Each of the 4 Option 4 redirects specifies which menu to re-present after Display Catalog completes
- One shared "## Display Catalog" section exists with Step 1 (refresh), Step 2 (dynamic render), Step 3 (fallback), Step 4 (return)
- The 4 duplicate catalog blocks are replaced by single-line redirects to one shared Display Catalog section (structural deduplication)
- Context Budget Discipline updated with PERF-03 exception for Display Catalog
- Hardcoded fallback includes toolkit-discovery as 4th skill
- Keystone tools section is hardcoded within display logic (not registry-sourced)
- wizard.md is ~230 lines shorter than before (4x78 duplicate lines removed, ~80 lines of shared block added)
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate parity and structural integrity</name>
  <files>skills/wizard.md</files>
  <action>
After Task 1 completes, run a validation pass to confirm the edit is correct.

**Validation 1: Parity check**
Count unique Keystone tool names in the Display Catalog section (both dynamic Keystone subsections and fallback block). All 18 must be present:

Agents (11): project-setup-advisor, project-setup-wizard, bmad-gsd-orchestrator, context-health-monitor, doc-shard-bridge, phase-gate-validator, admin-docs-agent, godot-dev-agent, it-infra-agent, open-source-agent, stack-update-watcher

Skills (4): wizard, wizard-backing-agent, wizard-detect, toolkit-discovery

Hooks (3): session-start, stack-update-banner, post-write-check

Run bash: for each of the 18 names, grep the Display Catalog section to confirm presence. If any missing, add it.

**Validation 2: No duplicate catalog blocks**
Confirm that the hardcoded catalog text (the block starting with "### Agents" and containing "#### Entry", "#### Bridge", etc.) appears exactly ONCE in the file (inside the fallback). It must NOT appear in the 4 Option 4 handler locations.

Run: `grep -c "#### Entry" skills/wizard.md` -- must return 1 (was 4 before).

**Validation 3: Option 4 redirects**
Confirm all 4 Option 4 handlers now contain "Go to ## Display Catalog":

Run: `grep -c "Go to ## Display Catalog" skills/wizard.md` -- must return 4.

**Validation 4: Re-present mechanism**
Confirm all 4 Option 4 redirects include "re-present" instruction naming a specific menu:

Run: `grep "Go to ## Display Catalog" skills/wizard.md | grep -c "re-present"` -- must return 4.

**Validation 5: KEYSTONE_NAMES completeness**
Confirm the KEYSTONE_NAMES set in the Display Catalog instruction text contains all 18 names.

**Validation 6: Context Budget Discipline updated**
Confirm the PERF-03 exception text is present and the old "Never read toolkit-registry.json" prohibition is removed.

Run: `grep "PERF-03" skills/wizard.md` -- must return a match.
Run: `grep "Never read toolkit-registry.json" skills/wizard.md` -- must return NO match.

**Validation 7: File parseable**
Confirm wizard.md YAML frontmatter is still valid:
Run: `head -20 skills/wizard.md` and verify the YAML block is intact with all required fields (name, description, model, tools, maxTurns).

If any validation fails, fix the issue in wizard.md before marking done.
  </action>
  <verify>
    <automated>NAMES="project-setup-advisor project-setup-wizard bmad-gsd-orchestrator context-health-monitor doc-shard-bridge phase-gate-validator admin-docs-agent godot-dev-agent it-infra-agent open-source-agent stack-update-watcher wizard wizard-backing-agent wizard-detect toolkit-discovery session-start stack-update-banner post-write-check"; MISSING=0; for n in $NAMES; do grep -q "$n" skills/wizard.md || { echo "MISSING: $n"; MISSING=$((MISSING+1)); }; done; ENTRY=$(grep -c "#### Entry" skills/wizard.md); REDIR=$(grep -c "Go to ## Display Catalog" skills/wizard.md); REPRES=$(grep "Go to ## Display Catalog" skills/wizard.md | grep -c "re-present"); PERF=$(grep -c "PERF-03" skills/wizard.md); NEVER=$(grep -c "Never read toolkit-registry.json" skills/wizard.md); echo "Missing=$MISSING Entry=$ENTRY Redirects=$REDIR RePresent=$REPRES PERF03=$PERF NeverLine=$NEVER"; [ "$MISSING" -eq 0 ] && [ "$ENTRY" -eq 1 ] && [ "$REDIR" -eq 4 ] && [ "$REPRES" -eq 4 ] && [ "$PERF" -ge 1 ] && [ "$NEVER" -eq 0 ] && echo "ALL PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- All 18 Keystone tool names present in Display Catalog section
- "#### Entry" appears exactly 1 time (in fallback only, not in 4 old handler locations)
- 4 Option 4 redirects pointing to ## Display Catalog
- All 4 redirects include "re-present" instruction naming the specific return menu
- PERF-03 exception present in Context Budget Discipline
- Old "Never read toolkit-registry.json" prohibition removed
- YAML frontmatter intact and valid
  </done>
</task>

</tasks>

<verification>
Phase-level verification against ROADMAP success criteria:

1. **SC #1 (Tool count matches install):** Display Catalog Step 2 reads counts from toolkit-registry.json -- the summary header shows the actual `counts.agents` value from the registry (e.g., 176), not the hardcoded 11.

2. **SC #2 (Stage-first grouping with category):** Display Catalog renders 4 stage sections (Research/Planning/Execution/Review), each with Keystone subsection and Installed tools subsection grouped by type (Agents/Skills/MCP).

3. **SC #3 (Hardcoded fallback):** Display Catalog Step 3 contains the full hardcoded Phase 7 catalog (with toolkit-discovery added as 4th skill). Triggered when registry file is missing or JSON parse fails.

4. **SC #4 (Parity check):** Task 2 validates all 18 Keystone tools appear in both the dynamic Keystone sections and the hardcoded fallback. Note: parity is verified at plan execution time, not at runtime.
</verification>

<success_criteria>
- wizard.md contains exactly 1 "## Display Catalog" section (matched by anchored `^## Display Catalog$`)
- wizard.md contains exactly 4 "Go to ## Display Catalog" redirects (one per Option 4 handler)
- All 4 redirects include "re-present" instruction naming which menu to return to
- wizard.md contains exactly 1 "#### Entry" occurrence (in fallback only)
- All 18 Keystone tool names present in wizard.md
- Context Budget Discipline mentions PERF-03 exception
- wizard.md YAML frontmatter is intact
- wizard.md is ~200-250 lines shorter than the original 664-line version
</success_criteria>

<output>
After completion, create `.planning/phases/15-dynamic-catalog-display/15-01-SUMMARY.md`
</output>
