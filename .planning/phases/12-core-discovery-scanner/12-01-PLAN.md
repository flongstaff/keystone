---
phase: 12-core-discovery-scanner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/toolkit-discovery.sh
  - .gitignore
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05
  - MATCH-01
  - MATCH-02
  - PERF-01

must_haves:
  truths:
    - "Running bash skills/toolkit-discovery.sh produces valid JSON to stdout and writes .claude/toolkit-registry.json"
    - "Registry contains all agents from ~/.claude/agents/, all skills from ~/.claude/skills/, all registered hooks from ~/.claude/settings.json hooks config, and all MCP servers from both ~/.claude/settings.json mcpServers AND ~/.claude/installed_plugins.json"
    - "Every discovered entry has a non-empty stages[] array -- keyword-matched entries get matching stages, zero-match entries and all MCP entries get all four stages"
    - "Compact summary stdout output contains counts per type and stage-grouped tool names capped at 8 per stage"
    - "Re-running within 1 hour returns cached result in under 0.1 seconds"
    - "Running when ~/.claude/agents/ does not exist produces valid empty-catalog JSON with exit 0"
    - "toolkit-registry.json is gitignored"
  artifacts:
    - path: "skills/toolkit-discovery.sh"
      provides: "Full toolkit scanner with TTL caching"
      min_lines: 150
    - path: ".gitignore"
      provides: "toolkit-registry.json exclusion"
      contains: "toolkit-registry.json"
  key_links:
    - from: "skills/toolkit-discovery.sh"
      to: "~/.claude/agents/*.md"
      via: "YAML frontmatter parsing"
      pattern: "agents/.*\\.md"
    - from: "skills/toolkit-discovery.sh"
      to: "~/.claude/settings.json"
      via: "mcpServers extraction and hooks registration extraction"
      pattern: "mcpServers|hooks"
    - from: "skills/toolkit-discovery.sh"
      to: "~/.claude/installed_plugins.json"
      via: "marketplace plugin scanning"
      pattern: "installed_plugins\\.json"
    - from: "skills/toolkit-discovery.sh"
      to: ".claude/toolkit-registry.json"
      via: "JSON write on scan completion"
      pattern: "toolkit-registry\\.json"
---

<objective>
Build the toolkit-discovery.sh script that scans the user's full installed toolkit (agents, skills, hooks, MCP servers), applies stage tagging via keyword matching, writes the full registry to .claude/toolkit-registry.json, and emits a compact summary to stdout with TTL-gated caching.

Purpose: This is the foundation for all v1.1 dynamic toolkit awareness -- every downstream phase (state integration, subagent injection, dynamic catalog) depends on this script producing a reliable, well-formed registry.
Output: skills/toolkit-discovery.sh (executable script), updated .gitignore
</objective>

<execution_context>
@/Users/flong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-core-discovery-scanner/12-CONTEXT.md

@skills/wizard-detect.sh

<interfaces>
<!-- The script creates a NEW file -- no existing interfaces to consume.
     Below are the contracts it must produce, derived from CONTEXT.md decisions. -->

Registry output schema (.claude/toolkit-registry.json):
```json
{
  "schema_version": 1,
  "scanned_at": "2026-03-13T14:35:07Z",
  "counts": {
    "agents": 160,
    "skills": 30,
    "hooks": 5,
    "mcp": 10
  },
  "tools": [
    {
      "name": "project-setup-wizard",
      "type": "agent",
      "description": "Use this agent at the START of any project...",
      "stages": ["execution"],
      "source": "~/.claude/agents/project-setup-wizard.md",
      "model": "sonnet",
      "tools": ["Read", "Write", "Bash", "Glob", "Grep"],
      "maxTurns": 30
    },
    {
      "name": "adapt",
      "type": "skill",
      "description": "Adapt designs to work across...",
      "stages": ["execution"],
      "source": "~/.claude/skills/adapt/SKILL.md",
      "trigger": "adapt"
    },
    {
      "name": "session-start",
      "type": "hook",
      "description": "SessionStart hook",
      "stages": ["research", "planning", "execution", "review"],
      "source": "~/.claude/settings.json#hooks",
      "event": "SessionStart"
    },
    {
      "name": "context7",
      "type": "mcp",
      "description": "context7 MCP server",
      "stages": ["research", "planning", "execution", "review"],
      "source": "mcpServers"
    }
  ]
}
```

Compact summary output (stdout):
```json
{
  "version": 1,
  "counts": { "agents": 160, "skills": 30, "hooks": 5, "mcp": 10 },
  "by_stage": {
    "research": ["context7", "code-reviewer", ...],
    "planning": ["bmad-gsd-orchestrator", ...],
    "execution": ["project-setup-wizard", ...],
    "review": ["phase-gate-validator", ...]
  }
}
```
Each stage list capped at 8 names. Priority: Keystone agents first, GSD agents, then alphabetical.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create toolkit-discovery.sh with full scanning, stage tagging, registry write, compact summary, and TTL caching</name>
  <files>skills/toolkit-discovery.sh, .gitignore</files>
  <action>
Create `skills/toolkit-discovery.sh` as an executable bash script. Use `set -euo pipefail`. The script must:

**1. TTL Cache Check (PERF-01)**
- Registry path: `.claude/toolkit-registry.json` (project-local)
- Check if registry file exists and its modification time is within 3600 seconds (1 hour)
- Use `stat -f %m` on macOS, `stat -c %Y` on Linux for file mtime (try macOS first, fallback to Linux)
- If cache is fresh AND `--force` flag is NOT set: extract compact summary from cached registry using python3 (read JSON, build summary), print to stdout, exit 0
- If cache is stale, missing, or `--force`: proceed to full scan

**2. Agent Scanning (DISC-01)**
- Scan `~/.claude/agents/*.md`
- If directory does not exist, set agents to empty array
- For each .md file, extract YAML frontmatter (text between first `---` and second `---`)
- Parse frontmatter fields: name, description (may be multi-line with `>`), model, tools (YAML list), maxTurns
- Use python3 for YAML-like parsing: read frontmatter block, extract fields with simple line parsing (do NOT require PyYAML -- use string splitting on `:` for single-line fields, handle `>` continuation for description, parse `- item` lists for tools)
- Build JSON entry: `{ name, type: "agent", description, stages: [], source, model, tools: [], maxTurns }`

**3. Skill Scanning (DISC-03)**
- Scan `~/.claude/skills/*/SKILL.md` (directories containing SKILL.md)
- Also scan `~/.claude/skills/*.md` for standalone skill files (like wizard.md, wizard-backing-agent.md) -- these are NOT skills for the registry; skip standalone .md files that do NOT have a SKILL.md in a subdirectory
- If directory does not exist, set skills to empty array
- For each SKILL.md, extract YAML frontmatter: name, description
- Build JSON entry: `{ name, type: "skill", description, stages: [], source, trigger: name }`

**4. Hook Scanning (DISC-04)**
- **CRITICAL per CONTEXT.md locked decision:** Only registered hooks count as installed. Unregistered scripts sitting in `~/.claude/hooks/` are IGNORED. The source of truth is `~/.claude/settings.json` hooks registrations, NOT the filesystem.
- Read `~/.claude/settings.json` and extract the `hooks` object. This object maps event types (e.g., "PreToolUse", "PostToolUse", "SessionStart") to arrays of hook registration objects. Each registration has a `command` string (the script path or command to run) and optionally a `matcher` field.
- For each registered hook entry across all event types: extract the hook name from the command string (derive from the script filename -- e.g., `/path/to/session-start.sh` becomes `session-start`), and record the event type it's registered under.
- If `~/.claude/settings.json` does not exist or has no `hooks` key, set hooks to empty array.
- Do NOT scan `~/.claude/hooks/` directory directly -- files in that directory that are not referenced in settings.json hook registrations must be excluded.
- Build JSON entry: `{ name, type: "hook", description: "{event} hook", stages: [], source: "~/.claude/settings.json#hooks", event }`

**5. MCP Server Scanning (DISC-02)**
- Scan TWO sources for MCP servers and merge results, deduplicating by name:
- **Source A: `~/.claude/settings.json` mcpServers section** -- extract the `mcpServers` object. Each key is a server name, each value is the server configuration object. Include all servers present in the mcpServers object (keys = server names).
- **Source B: `~/.claude/installed_plugins.json`** -- if this file exists, read and parse it. Extract plugin entries (the structure may be an array of objects or a map -- inspect the actual file structure and adapt). Extract the plugin name from each entry.
- Merge both sources: combine all discovered MCP server/plugin names. Deduplicate by name (if a server appears in both mcpServers and installed_plugins.json, include it once).
- Build JSON entry for each unique MCP server: `{ name: (server name), type: "mcp", description: "{name} MCP server", stages: ["research", "planning", "execution", "review"], source: "mcpServers" }` (source: "mcpServers" if from settings.json mcpServers, "installed_plugins.json" if only from marketplace, "mcpServers" if from both)
- **CRITICAL per CONTEXT.md locked decision:** ALL MCP entries get `stages: ["research", "planning", "execution", "review"]` -- MCP descriptions are too minimal for reliable keyword matching, so they bypass stage tagging and receive all four stages unconditionally.

**6. Stage Tagging (MATCH-01)**
- After all entries are collected, apply keyword matching to each NON-MCP entry's description field (MCP entries already have all stages assigned in step 5 -- skip them here)
- Four stage keyword lists (pipe-delimited for grep -iE):
  - research: `research|investigate|analyze|explore|scan|audit|inspect|fetch|crawl`
  - planning: `plan|design|architect|strategy|roadmap|scope|requirement|scaffold`
  - execution: `execute|implement|build|write|deploy|create|generate|fix|develop`
  - review: `review|test|validate|verify|check|lint|quality|security`
- For each entry, test description against each stage's pattern. If match, add stage to stages array
- Also match against the name field as a secondary signal (e.g., "code-reviewer" should match review)
- **Zero-match fallback per CONTEXT.md locked decision:** Entries with no keyword match get ALL four stages assigned: `["research", "planning", "execution", "review"]` -- better to over-surface than miss a useful tool

**7. Registry Write (DISC-05)**
- Build complete registry JSON with a FLAT tools array per CONTEXT.md locked decision: `{ schema_version: 1, scanned_at: ISO_TIMESTAMP, counts: {agents, skills, hooks, mcp}, tools: [...all_entries] }`
- The `tools` array contains ALL entries (agents, skills, hooks, MCP) in a single flat list. Each entry has a `type` field ("agent", "skill", "hook", "mcp") for filtering.
- Ensure `.claude/` directory exists (`mkdir -p .claude`)
- Write to `.claude/toolkit-registry.json` using python3 json.dumps with indent=2
- Use python3 for ALL JSON construction (not heredoc cat) -- the registry is complex with arrays of objects containing arrays, and proper escaping of description strings with quotes/special chars is critical for 160+ agents

**8. Compact Summary (MATCH-02)**
- After writing registry, build compact summary JSON for stdout
- Summary: `{ version: 1, counts: {agents, skills, hooks, mcp}, by_stage: {research: [...], planning: [...], execution: [...], review: [...]} }`
- Each stage list: collect all entry names that have that stage, then sort with priority: entries whose source contains "keystone" or whose name matches known Keystone agents (wizard, bmad-gsd-orchestrator, context-health-monitor, phase-gate-validator, doc-shard-bridge, project-setup-wizard, project-setup-advisor, it-infra-agent, godot-dev-agent, open-source-agent, admin-docs-agent, stack-update-watcher) first, then GSD-related names, then alphabetical
- Cap each stage list at 8 names (MATCH-02)
- Target total summary size: ~500-600 bytes
- Print summary JSON to stdout (this is what wizard-detect.sh will capture in Phase 13)

**Implementation approach:**
- Use a single python3 inline script (invoked via `python3 -c "..."`) for the heavy lifting: YAML frontmatter parsing, JSON construction, stage tagging, and summary generation. The bash wrapper handles: argument parsing (`--force`), TTL cache check, directory existence checks, and calling the python3 block
- This mirrors the pattern in wizard-detect.sh where python3 is used inline for JSON manipulation
- The python3 block should read directories and files directly (using os, pathlib) rather than receiving data from bash pipes -- this is more robust for 160+ agents

**Error handling:**
- Missing directories (~/.claude/agents/, ~/.claude/skills/): produce empty arrays, no error exit
- Missing ~/.claude/settings.json: produce empty hooks array AND empty MCP array (from mcpServers source)
- Missing ~/.claude/installed_plugins.json: skip that source, no error (only use mcpServers from settings.json)
- Malformed YAML frontmatter in an agent file: skip that entry, continue scanning (log warning to stderr)
- Script must always exit 0 (consumed by wizard pipeline)
- Error output to stderr, structured data (compact summary) to stdout only

**Also update `.gitignore`:**
- Add `toolkit-registry.json` entry under a new comment section `# Toolkit discovery (machine-specific)`
- Place after the existing `# Local configuration` section

Make the script executable: `chmod +x skills/toolkit-discovery.sh`
  </action>
  <verify>
    <automated>bash skills/toolkit-discovery.sh --force 2>/dev/null | python3 -m json.tool > /dev/null && echo "PASS: valid JSON stdout" || echo "FAIL: invalid JSON stdout"; [ -f .claude/toolkit-registry.json ] && python3 -m json.tool .claude/toolkit-registry.json > /dev/null && echo "PASS: valid registry JSON" || echo "FAIL: no registry or invalid JSON"; grep -q "toolkit-registry.json" .gitignore && echo "PASS: gitignored" || echo "FAIL: not gitignored"</automated>
  </verify>
  <done>
    - skills/toolkit-discovery.sh exists and is executable
    - Running with --force produces valid JSON on stdout (compact summary)
    - .claude/toolkit-registry.json is written with valid JSON containing schema_version, scanned_at, counts, and flat tools array
    - Agent count in registry matches `ls ~/.claude/agents/*.md 2>/dev/null | wc -l`
    - All MCP entries have stages set to all four stages ["research", "planning", "execution", "review"]
    - All zero-keyword-match entries have stages set to all four stages (no empty stages arrays in the registry)
    - MCP entries include servers from both ~/.claude/settings.json mcpServers and ~/.claude/installed_plugins.json (if it exists), deduplicated by name
    - Hook entries come from settings.json hook registrations only (not filesystem scan of ~/.claude/hooks/)
    - toolkit-registry.json appears in .gitignore
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate discovery against real toolkit and verify caching behavior</name>
  <files>skills/toolkit-discovery.sh</files>
  <action>
Run the script against the real installed toolkit and validate all requirements:

**Validation 1: Agent count matches (DISC-01)**
- Run `ls ~/.claude/agents/*.md 2>/dev/null | wc -l` to get expected count
- Run `bash skills/toolkit-discovery.sh --force` and extract agents count from registry
- Compare counts -- they must match

**Validation 2: MCP server count (DISC-02)**
- Extract MCP server count from `~/.claude/settings.json` mcpServers (count keys in the mcpServers object)
- Also check if `~/.claude/installed_plugins.json` exists and count entries from there
- Compute expected total unique MCP count (union of both sources, deduplicated by name)
- Compare with mcp count in registry -- they must match

**Validation 3: Skills count (DISC-03)**
- Count directories in `~/.claude/skills/` that contain SKILL.md
- Compare with skills count in registry

**Validation 4: Hooks count (DISC-04)**
- Count registered hooks from `~/.claude/settings.json` hooks section (not from filesystem)
- Count total hook registration entries across all event types in settings.json hooks object
- Compare with hooks count in registry -- they must match
- **Verify exclusion:** If any .sh/.js files exist in `~/.claude/hooks/` that are NOT referenced in settings.json hook registrations, confirm they do NOT appear in the registry

**Validation 5: Stage tagging works (MATCH-01)**
- Check that ALL entries have non-empty stages arrays (no entry should have stages: [])
- Specifically verify that known agents have expected stages (e.g., code-reviewer should have "review", bmad-gsd-orchestrator should have "planning" or "execution")
- Verify ALL MCP entries have all four stages ["research", "planning", "execution", "review"]

**Validation 6: Compact summary size (MATCH-02)**
- Measure stdout output size in bytes (`bash skills/toolkit-discovery.sh --force 2>/dev/null | wc -c`)
- Must be under 800 bytes (target 500-600)
- Each stage list must have at most 8 entries

**Validation 7: TTL caching (PERF-01)**
- Run `bash skills/toolkit-discovery.sh` (no --force) -- should use cache from previous run
- Time the execution: `time bash skills/toolkit-discovery.sh 2>/dev/null`
- Second run must complete in under 0.5 seconds (target 0.1s)

**Validation 8: Empty directory handling (DISC-05 edge case)**
- Temporarily test with a non-existent agents directory by setting an override or by verifying the script's code path handles `[ -d ... ]` checks

**Validation 9: Registry schema**
- Verify registry has: schema_version (number, value 1), scanned_at (ISO string), counts (object), tools (array)
- Verify the tools array is flat -- all entries in one array with per-entry type field
- Verify at least one agent entry has: name (string), type ("agent"), description (string), stages (array), source (string), model (string)
- Verify NO top-level keys named "agents", "skills", "hooks", or "mcp" exist (the flat tools array is the only entry container)

**Validation 10: Zero-match fallback**
- Verify that no entry in the entire registry has an empty stages array
- Any entry with `stages: []` is a bug -- the zero-match fallback should have assigned all four stages

If any validation fails, fix the script to make it pass. Common issues to watch for:
- Description field truncation breaking JSON (quotes in descriptions)
- YAML multi-line description (`>` or `|`) not parsed correctly
- tools field parsed as string instead of array
- Stage keywords matching partial words in unexpected places (e.g., "explore" in "explorer" is fine, but "plan" in "transplant" is not -- use word boundaries if this causes false positives)
- MCP entries missing all-stages assignment
- installed_plugins.json not being read or merged
- Hooks from filesystem scan instead of settings.json registrations
  </action>
  <verify>
    <automated>AGENT_EXPECTED=$(ls ~/.claude/agents/*.md 2>/dev/null | wc -l | tr -d ' '); AGENT_ACTUAL=$(python3 -c "import json; print(json.load(open('.claude/toolkit-registry.json'))['counts']['agents'])" 2>/dev/null); [ "$AGENT_EXPECTED" = "$AGENT_ACTUAL" ] && echo "PASS: agent count $AGENT_ACTUAL matches" || echo "FAIL: expected $AGENT_EXPECTED got $AGENT_ACTUAL"; SUMMARY_SIZE=$(bash skills/toolkit-discovery.sh 2>/dev/null | wc -c | tr -d ' '); [ "$SUMMARY_SIZE" -lt 800 ] && echo "PASS: summary ${SUMMARY_SIZE}B" || echo "FAIL: summary ${SUMMARY_SIZE}B too large"; EMPTY_STAGES=$(python3 -c "import json; r=json.load(open('.claude/toolkit-registry.json')); print(sum(1 for e in r['tools'] if not e['stages']))" 2>/dev/null); [ "$EMPTY_STAGES" = "0" ] && echo "PASS: no empty stages arrays" || echo "FAIL: $EMPTY_STAGES entries have empty stages"; MCP_ALL_STAGES=$(python3 -c "import json; r=json.load(open('.claude/toolkit-registry.json')); print(all(sorted(e['stages'])==['execution','planning','research','review'] for e in r['tools'] if e['type']=='mcp'))" 2>/dev/null); [ "$MCP_ALL_STAGES" = "True" ] && echo "PASS: all MCP entries have all stages" || echo "FAIL: MCP entries missing stages"; SCHEMA_VER=$(python3 -c "import json; print(json.load(open('.claude/toolkit-registry.json')).get('schema_version', 'MISSING'))" 2>/dev/null); [ "$SCHEMA_VER" = "1" ] && echo "PASS: schema_version=1" || echo "FAIL: schema_version=$SCHEMA_VER"; HAS_FLAT=$(python3 -c "import json; r=json.load(open('.claude/toolkit-registry.json')); print('tools' in r and isinstance(r['tools'], list) and 'agents' not in r)" 2>/dev/null); [ "$HAS_FLAT" = "True" ] && echo "PASS: flat tools array" || echo "FAIL: not flat tools array"</automated>
  </verify>
  <done>
    - Agent count in registry matches actual agent file count on disk
    - MCP server count matches union of mcpServers from settings.json and installed_plugins.json (deduplicated)
    - Skills count matches filesystem (directories with SKILL.md)
    - Hooks count matches registered hooks in settings.json (not filesystem file count)
    - Stage tagging produces non-empty stages for ALL entries (no empty stages arrays anywhere)
    - All MCP entries have all four stages ["research", "planning", "execution", "review"]
    - Compact summary under 800 bytes with stage lists capped at 8
    - Cached second run completes in under 0.5 seconds
    - Registry uses schema_version: 1 (not version: 1) and flat tools array (not separate typed arrays)
    - Script handles missing directories gracefully (no error exit)
  </done>
</task>

</tasks>

<verification>
1. `bash skills/toolkit-discovery.sh --force 2>/dev/null | python3 -m json.tool` produces valid compact summary JSON
2. `python3 -m json.tool .claude/toolkit-registry.json` produces valid full registry JSON
3. Agent count in registry equals `ls ~/.claude/agents/*.md | wc -l`
4. No entry in the registry has an empty stages array (zero-match fallback assigns all four stages)
5. All MCP entries have stages ["research", "planning", "execution", "review"]
6. MCP count includes servers from both settings.json mcpServers and installed_plugins.json (deduplicated)
7. Hooks count matches settings.json hook registrations (unregistered scripts in ~/.claude/hooks/ are excluded)
8. Second run (cached) completes in under 0.5 seconds
9. `grep toolkit-registry.json .gitignore` returns a match
10. Running in a directory where `~/.claude/agents/` does not exist produces valid empty-catalog JSON (counts all 0)
11. Registry top-level key is `schema_version` (not `version`)
12. Registry uses flat `tools` array (not separate `agents`/`skills`/`hooks`/`mcp` arrays)
</verification>

<success_criteria>
- toolkit-discovery.sh scans all 4 tool types (agents, skills, hooks, MCP servers) and produces correct counts
- MCP scanning covers both ~/.claude/settings.json mcpServers and ~/.claude/installed_plugins.json, deduplicated
- Hook scanning uses settings.json hook registrations as source of truth (unregistered scripts in ~/.claude/hooks/ are ignored)
- Every entry in the registry carries a non-empty stages[] array -- keyword-matched or all-stages fallback
- All MCP entries unconditionally receive all four stages (per locked decision)
- Full registry written to .claude/toolkit-registry.json with schema_version: 1 and flat tools array; compact summary emitted to stdout
- TTL caching skips rescan when registry is fresh (observable: < 0.5s on second run)
- Empty/missing directories produce valid empty-catalog JSON with exit 0
- toolkit-registry.json is gitignored
</success_criteria>

<output>
After completion, create `.planning/phases/12-core-discovery-scanner/12-01-SUMMARY.md`
</output>
