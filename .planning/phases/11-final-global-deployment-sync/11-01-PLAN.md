---
phase: 11-final-global-deployment-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/wizard.md
  - ~/.claude/skills/wizard.md
  - ~/.claude/skills/wizard-backing-agent.md
  - ~/.claude/skills/wizard-detect.sh
autonomous: true
requirements:
  - "REGRESSION-GUARD: closes integration gap #14 and flow gap from v1.0 final audit"

must_haves:
  truths:
    - "wizard.md gsd-only non-uat-passing Option 3 cross-references full-stack Option 3 (not Option 4)"
    - "Global wizard-detect.sh matches project-local (includes VERIFICATION.md ladder step from Phase 10)"
    - "Global wizard.md matches project-local (includes Option 3 fix + complete status handling from Phase 10)"
    - "Global wizard-backing-agent.md matches project-local (includes Route C sync note from Phase 10)"
    - "Zero diff between all project-local and global skill files"
  artifacts:
    - path: "skills/wizard.md"
      provides: "Fixed Option 3 cross-reference at line 321"
      contains: "Option 3 (Validate phase).*Option 3 above"
    - path: "~/.claude/skills/wizard.md"
      provides: "Global deployment of wizard with label fix + Phase 10 changes"
    - path: "~/.claude/skills/wizard-backing-agent.md"
      provides: "Global deployment of backing agent with Phase 10 Route C sync note"
    - path: "~/.claude/skills/wizard-detect.sh"
      provides: "Global deployment of detection script with VERIFICATION.md ladder"
  key_links:
    - from: "skills/wizard.md"
      to: "~/.claude/skills/wizard.md"
      via: "cp -p (byte-for-byte copy with permission preservation)"
      pattern: "diff exits 0"
    - from: "skills/wizard-backing-agent.md"
      to: "~/.claude/skills/wizard-backing-agent.md"
      via: "cp -p"
      pattern: "diff exits 0"
    - from: "skills/wizard-detect.sh"
      to: "~/.claude/skills/wizard-detect.sh"
      via: "cp -p"
      pattern: "diff exits 0"
---

<objective>
Fix the stale "Option 4" cross-reference label in wizard.md and redeploy all 3 Keystone skill files to ~/.claude/skills/, achieving zero diff between project-local and global copies.

Purpose: Closes the final v1.0 milestone audit gaps — one flow gap (wrong label), one integration gap (Phase 10 changes not deployed globally), and one tech debt item (stale global files).
Output: All 3 global skill files match project-local; label bug fixed.
</objective>

<execution_context>
@/Users/flong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-final-global-deployment-sync/11-CONTEXT.md
@.planning/phases/11-final-global-deployment-sync/11-RESEARCH.md
@.planning/phases/09-global-deployment-sync/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Option 3 label and deploy all skill files globally</name>
  <files>skills/wizard.md, ~/.claude/skills/wizard.md, ~/.claude/skills/wizard-backing-agent.md, ~/.claude/skills/wizard-detect.sh</files>
  <action>
Two sequential operations: fix label, then copy all files.

**Step 1 — Fix the label bug in project-local wizard.md:**

Edit `skills/wizard.md` line 321. Change:
```
- **Option 3 (Validate phase):** Same as full-stack Option 4 above.
```
to:
```
- **Option 3 (Validate phase):** Same as full-stack Option 3 above.
```

This is a one-word change: "Option 4" becomes "Option 3". The cross-reference is in the gsd-only non-uat-passing menu section. The full-stack non-uat-passing menu has "Validate phase" as Option 3 (line ~168), not Option 4. Lines 96, 173, 255, 323 contain legitimate "Option 4 (Discover tools)" entries — do NOT change those.

After the edit, verify no "Option 4" reference remains at line 321:
```bash
grep -n "Option 4" skills/wizard.md | grep "321"
```
Expected: no output (the bug is gone).

Confirm the fix is correct:
```bash
grep -n "Option 3 (Validate phase)" skills/wizard.md | grep "321"
```
Expected: `321:- **Option 3 (Validate phase):** Same as full-stack Option 3 above.`

**Step 2 — Copy all 3 skill files from project-local to global:**

Use `cp -p` (preserves permissions including executable bit on wizard-detect.sh). Use absolute paths — do NOT quote the tilde (quoting suppresses expansion). Source is always project-local, destination is always global.

```bash
cp -p /Users/flong/Developer/keystone/skills/wizard.md ~/.claude/skills/wizard.md
cp -p /Users/flong/Developer/keystone/skills/wizard-backing-agent.md ~/.claude/skills/wizard-backing-agent.md
cp -p /Users/flong/Developer/keystone/skills/wizard-detect.sh ~/.claude/skills/wizard-detect.sh
```

Copy ALL files regardless of individual diff status — idempotent, ensures completeness per user decision.

**Anti-patterns to avoid:**
- Do NOT fix the global file directly (cp -p would overwrite the fix with the still-broken local)
- Do NOT reverse source/destination (would overwrite project-local with stale global)
- Do NOT use cp without -p (would strip executable bit from wizard-detect.sh)
  </action>
  <verify>
    <automated>diff /Users/flong/Developer/keystone/skills/wizard.md /Users/flong/.claude/skills/wizard.md && diff /Users/flong/Developer/keystone/skills/wizard-backing-agent.md /Users/flong/.claude/skills/wizard-backing-agent.md && diff /Users/flong/Developer/keystone/skills/wizard-detect.sh /Users/flong/.claude/skills/wizard-detect.sh && test -x /Users/flong/.claude/skills/wizard-detect.sh && grep -q "Option 3 (Validate phase).*Option 3 above" /Users/flong/.claude/skills/wizard.md && echo "ALL PASS"</automated>
  </verify>
  <done>
    - Line 321 of skills/wizard.md reads "Same as full-stack Option 3 above" (not "Option 4")
    - All 3 global skill files are byte-for-byte identical to project-local (diff exits 0)
    - wizard-detect.sh executable bit is preserved in global copy
    - Label fix is confirmed present in global wizard.md via grep
  </done>
</task>

<task type="auto">
  <name>Task 2: Update VALIDATION.md with passing status</name>
  <files>.planning/phases/11-final-global-deployment-sync/11-VALIDATION.md</files>
  <action>
Update the existing `11-VALIDATION.md` to reflect completion:

1. Set frontmatter `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`
2. Update each per-task verification row status from `pending` to `green`
3. Check the final sign-off checkbox (`nyquist_compliant: true set in frontmatter`)
4. Set **Approval** to the current date

Use the Phase 9 VERIFICATION.md format as reference (at `.planning/phases/09-global-deployment-sync/09-VERIFICATION.md`).
  </action>
  <verify>
    <automated>grep -q "status: complete" /Users/flong/Developer/keystone/.planning/phases/11-final-global-deployment-sync/11-VALIDATION.md && grep -q "nyquist_compliant: true" /Users/flong/Developer/keystone/.planning/phases/11-final-global-deployment-sync/11-VALIDATION.md && echo PASS</automated>
  </verify>
  <done>
    - VALIDATION.md frontmatter shows status: complete and nyquist_compliant: true
    - All 5 per-task rows show green status
    - Sign-off checklist fully checked
  </done>
</task>

</tasks>

<verification>
Run the full verification suite (< 1 second):

```bash
diff /Users/flong/Developer/keystone/skills/wizard.md /Users/flong/.claude/skills/wizard.md && \
diff /Users/flong/Developer/keystone/skills/wizard-backing-agent.md /Users/flong/.claude/skills/wizard-backing-agent.md && \
diff /Users/flong/Developer/keystone/skills/wizard-detect.sh /Users/flong/.claude/skills/wizard-detect.sh && \
test -x /Users/flong/.claude/skills/wizard-detect.sh && \
grep -q "Option 3 (Validate phase).*Option 3 above" /Users/flong/.claude/skills/wizard.md && \
echo "ALL PASS"
```

All 5 success criteria from ROADMAP:
1. SC #1: `grep -n "Option 3 (Validate phase).*Option 3 above" skills/wizard.md` shows line 321
2. SC #2: `diff skills/wizard-detect.sh ~/.claude/skills/wizard-detect.sh` exits 0
3. SC #3: `diff skills/wizard.md ~/.claude/skills/wizard.md` exits 0
4. SC #4: `diff skills/wizard-backing-agent.md ~/.claude/skills/wizard-backing-agent.md` exits 0
5. SC #5: All three diffs exit 0 (composite of SC #2-4)
</verification>

<success_criteria>
- wizard.md line 321 says "Option 3" not "Option 4"
- All 3 global skill files show zero diff against project-local
- wizard-detect.sh executable bit preserved in global
- VALIDATION.md shows complete/passing status
</success_criteria>

<output>
After completion, create `.planning/phases/11-final-global-deployment-sync/11-01-SUMMARY.md`
</output>
