---
phase: 14-subagent-injection-confirmation-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/wizard.md
  - skills/wizard-backing-agent.md
autonomous: true
requirements: [INJ-02, INJ-03, INJ-04, CONF-01, CONF-02, CONF-03, PERF-03]

must_haves:
  truths:
    - "Known-safe tools inject into Agent()/Task() prompts without any confirmation prompt"
    - "Unknown user-installed tools trigger exactly one batched confirmation per /wizard invocation"
    - "MCP entries in capability blocks use 'configured -- availability may vary' language"
    - "Capability block does not appear as user-visible output in the wizard turn"
    - "Full toolkit-registry.json is never loaded during injection — only wizard-state.json toolkit.by_stage"
    - "Skill() invocations receive no injection"
  artifacts:
    - path: "skills/wizard.md"
      provides: "Step 2.5 confirmation logic, trust classification allowlist, capability block injection for Agent() spawns"
      contains: "Step 2.5"
    - path: "skills/wizard-backing-agent.md"
      provides: "Capability block injection before Task(bmad-gsd-orchestrator) spawn"
      contains: "capabilities"
  key_links:
    - from: "skills/wizard.md"
      to: ".claude/wizard-state.json"
      via: "reads toolkit.by_stage from Step 2 JSON data"
      pattern: "toolkit\\.by_stage"
    - from: "skills/wizard.md"
      to: "Agent() spawn prompts"
      via: "appends <capabilities> block to prompt string"
      pattern: "<capabilities>"
    - from: "skills/wizard-backing-agent.md"
      to: "Task(bmad-gsd-orchestrator)"
      via: "appends <capabilities> block to Task prompt"
      pattern: "<capabilities>"
---

<objective>
Add confirmation UX and capability injection to wizard.md and wizard-backing-agent.md

Purpose: The wizard needs to (1) classify discovered tools as known-safe vs unknown, (2) present a single batched confirmation when unknowns exist, and (3) inject stage-filtered `<capabilities>` blocks into all Agent() and Task() spawn prompts. This covers the wizard-side of injection (project-local skills files), while Plan 02 covers GSD workflow files separately.

Output: Modified wizard.md with Step 2.5 (trust classification + confirmation), injection instructions at all Agent() and Task() spawn points. Modified wizard-backing-agent.md with injection instruction before Task(bmad-gsd-orchestrator).
</objective>

<execution_context>
@/Users/flong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-subagent-injection-confirmation-ux/14-CONTEXT.md
@.planning/phases/14-subagent-injection-confirmation-ux/14-RESEARCH.md

@skills/wizard.md
@skills/wizard-backing-agent.md

<interfaces>
<!-- wizard-state.json toolkit structure (from Phase 13) -->
<!-- Executor needs to know the exact JSON shape being read -->

From .claude/wizard-state.json (populated by wizard-detect.sh):
```json
{
  "toolkit": {
    "counts": { "agents": 176, "skills": 28, "hooks": 24, "mcp": 0 },
    "by_stage": {
      "research": ["gsd-phase-researcher", "project-setup-wizard", "gsd-codebase-mapper", "gsd-context-monitor", "gsd-debugger", "gsd-nyquist-auditor"],
      "planning": ["gsd-planner", "gsd-plan-checker", "gsd-roadmapper", "gsd-phase-researcher", "project-setup-wizard", "gsd-codebase-mapper"],
      "execution": ["gsd-executor", "gsd-debugger", "gsd-context-monitor", "gsd-codebase-mapper", "gsd-nyquist-auditor", "gsd-phase-researcher"],
      "review": ["gsd-verifier", "gsd-plan-checker", "gsd-nyquist-auditor", "gsd-context-monitor", "gsd-phase-researcher", "gsd-codebase-mapper"]
    }
  }
}
```

<!-- Stage-to-subagent mapping (from CONTEXT.md locked decisions) -->
wizard.md Agent() spawn mapping:
  - context-health-monitor (Agent) -> by_stage.review
  - phase-gate-validator (Agent) -> by_stage.review
wizard-backing-agent.md Task() spawn mapping:
  - bmad-gsd-orchestrator (Task) -> by_stage.planning

<!-- Capability block format (from CONTEXT.md locked decisions) -->
```xml
<capabilities>
{stage preamble}
- {tool-name} -- {one-liner description}
- {mcp-name} (configured -- availability may vary) -- MCP server for {name}
</capabilities>
```

Stage preambles:
  - research: "Query these before investigating unknowns:"
  - planning: "Reference these during planning if relevant:"
  - execution: "Use these during implementation if relevant:"
  - review: "Use these for validation and checking:"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 2.5 trust classification and confirmation UX to wizard.md</name>
  <files>skills/wizard.md</files>
  <action>
Add a new section "## Step 2.5: Classify Toolkit and Confirm Unknown Tools" between Step 2 (Read wizard-state.json) and Step 3 (Branch on scenario) in wizard.md.

This step fires once per wizard invocation, BEFORE any scenario branch executes. It reads toolkit.by_stage from the wizard-state.json data already loaded in Step 2.

**Step 2.5 content:**

1. **Graceful skip guard:** If `toolkit` is missing, empty (`{}`), or `by_stage` is absent from wizard-state.json: skip Step 2.5 entirely — no classification, no confirmation, proceed to Step 3.

2. **Trust classification:** Collect all unique tool names across all by_stage arrays. Split into two buckets using a hardcoded KNOWN_SAFE allowlist:

   KNOWN_SAFE allowlist (hardcode this list in the step):
   ```
   gsd-executor, gsd-phase-researcher, gsd-planner, gsd-plan-checker, gsd-verifier,
   gsd-roadmapper, gsd-codebase-mapper, gsd-debugger, gsd-nyquist-auditor,
   gsd-context-monitor, gsd-project-researcher, gsd-research-synthesizer,
   bmad-gsd-orchestrator, context-health-monitor, doc-shard-bridge,
   phase-gate-validator, project-setup-wizard, stack-update-watcher,
   context7, deepwiki, open-source-agent, it-infra-agent, godot-dev-agent,
   admin-docs-agent
   ```

   - KNOWN_SAFE bucket: tool names found in the allowlist
   - UNKNOWN bucket: tool names NOT in the allowlist

3. **Confirmation logic:**
   - If UNKNOWN bucket is empty: set TOOLS_CONFIRMED = "all" (auto-approve, zero friction). Proceed to Step 3.
   - If UNKNOWN bucket is non-empty: present AskUserQuestion ONCE:

     Question: "New tools in toolkit"
     Body: "Your toolkit includes tools outside the Keystone/GSD standard set. Include them in capability hints sent to subagents?\n\nUnknown tools: {comma-separated list of unknown tool names}"
     Options:
       - "Allow all -- include {N} unknown tool(s) this session"
       - "Skip unknown tools -- Keystone/GSD tools only"
       - "Cancel -- don't proceed with action"

   - If "Allow all": set TOOLS_CONFIRMED = "all"
   - If "Skip unknown tools": set TOOLS_CONFIRMED = "safe-only"
   - If "Cancel": set TOOLS_CONFIRMED = "cancel"

4. **Cancel handling:** If TOOLS_CONFIRMED = "cancel" and the user selected an action that triggers a spawn, do not execute the spawn. Re-present the menu.

5. **Note:** TOOLS_CONFIRMED is held as a local variable for the duration of this wizard invocation. It is NOT written to wizard-state.json (wizard-detect.sh owns writes). Subsequent spawns within the same invocation reuse the stored answer — no repeat prompts.

**Also add a helper instruction block** (can be placed right after Step 2.5) called "### Build Capability Block" that describes HOW to construct the `<capabilities>` XML block. This block is referenced by all spawn points:

```
### Build Capability Block

When a spawn point says "build capability block for stage {STAGE}":

1. Read toolkit.by_stage.{STAGE} from wizard-state.json (already in context from Step 2)
2. If by_stage.{STAGE} is empty or missing: skip — do not append any block
3. If TOOLS_CONFIRMED is "safe-only": filter the array to only KNOWN_SAFE names
4. If TOOLS_CONFIRMED is "cancel": do not build block (spawn should not proceed)
5. For each tool name in the filtered array:
   - If name matches a known MCP tool (context7, deepwiki, or any non-agent/non-skill/non-hook):
     format as: `- {name} (configured -- availability may vary) -- MCP server for {name}`
   - Otherwise: format as: `- {name} -- {tool name as identifier}`
6. Wrap in XML block with stage-appropriate preamble:

<capabilities>
{preamble for stage}
{pointer lines}
</capabilities>

Stage preambles:
  - review: "Use these for validation and checking:"

7. Append this block to the Agent()/Task() prompt string, after any <files_to_read> section.
8. Do NOT display the capabilities block to the user — it is internal to the spawn prompt.
```

**Then modify each Agent() and Task() spawn point in wizard.md** to include "Build capability block for stage review" (or appropriate stage) before the spawn. The specific spawn points to modify:

a. **context-health-monitor Agent() spawn** (appears in full-stack uat-passing Option 1, full-stack non-uat Option 2, gsd-only uat-passing Option 1, gsd-only non-uat Option 2):
   Add instruction: "Before spawning, build capability block for stage 'review' and append to the Agent prompt string."

b. **phase-gate-validator Agent() spawn** (appears in full-stack uat-passing Option 3, full-stack non-uat Option 3, gsd-only uat-passing Option 3, gsd-only non-uat Option 3):
   Add instruction: "Before spawning, build capability block for stage 'review' and append to the Agent prompt string."

c. **bmad-ready scenario Task(wizard-backing-agent) spawn** (Option 1 Bridge):
   Add instruction: "Before spawning, build capability block for stage 'planning' and append to the Task prompt string."

Do NOT add injection to:
- Skill() invocations (show traceability uses Skill — no injection per INJ-04)
- Explain mode options (no spawns)
- The "none" scenario (no Agent/Task spawns that would benefit from toolkit awareness)
- The "ambiguous" scenario (cleanup/diagnostic, no relevant spawns)

**Context Budget Discipline update:** Add to the existing Context Budget Discipline section at the bottom:
"- The only toolkit data source is wizard-state.json toolkit.by_stage (already loaded in Step 2). Never read toolkit-registry.json from wizard.md."
  </action>
  <verify>
    <automated>grep -c 'Step 2.5' skills/wizard.md && grep -c 'KNOWN_SAFE' skills/wizard.md && grep -c '<capabilities>' skills/wizard.md && grep -c 'toolkit-registry' skills/wizard.md | grep -q '^0$' && echo "PASS: Step 2.5 present, KNOWN_SAFE present, capabilities present, no registry reference" || echo "FAIL"</automated>
  </verify>
  <done>
wizard.md contains Step 2.5 with trust classification and confirmation UX. All Agent() and Task() spawn points have injection instructions. No Skill() invocations have injection. toolkit-registry.json is not referenced. KNOWN_SAFE allowlist is hardcoded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add capability injection to wizard-backing-agent.md before bridge Task() spawn</name>
  <files>skills/wizard-backing-agent.md</files>
  <action>
Add a new step "### Step 2.5 -- Build Bridge Capability Block" in wizard-backing-agent.md, between Step 2 (Prompt before bridging) and Step 3 (Delegate to bmad-gsd-orchestrator via Task()).

This step instructs the backing agent to build a `<capabilities>` block using by_stage.planning from wizard-state.json (already read in Route Dispatch).

**Step 2.5 content:**

```markdown
### Step 2.5 -- Build Bridge Capability Block

If wizard-state.json contains non-empty `toolkit.by_stage.planning`:

1. Read toolkit.by_stage.planning (array of tool names, already in context from Route Dispatch read)
2. For each tool name:
   - MCP tools (context7, deepwiki, or any name that is not a known agent/skill/hook):
     `- {name} (configured -- availability may vary) -- MCP server for {name}`
   - All others: `- {name} -- {tool name as identifier}`
3. Build:

<capabilities>
Reference these during planning if relevant:
{pointer lines}
</capabilities>

4. Append this block to the Task() prompt in Step 3, after the Operation A instruction line.

If wizard-state.json is missing, toolkit is `{}`, or by_stage.planning is empty: skip -- do not append any block. The bridge proceeds normally without capability hints.
```

**Then update Step 3** (Delegate to bmad-gsd-orchestrator via Task()) to note the capability block:

In the Task invocation, update the prompt to include a note:
- **prompt:** "Read agents/bridge/bmad-gsd-orchestrator.md and follow Operation A to initialise GSD from BMAD docs.{capability_block_if_built}"

**Also add a rule** to the existing Rules section at the bottom:
"- **Never read toolkit-registry.json.** Injection uses only wizard-state.json toolkit.by_stage (compact summary). Full registry is reserved for 'Discover tools' display (PERF-03)."

**Do NOT inject into Route C** (traceability display). Route C does not spawn any Task()/Agent() — it reads files and displays output directly. Injecting into Route C would violate INJ-04's principle (inject only into spawn calls).
  </action>
  <verify>
    <automated>grep -c 'Step 2.5' skills/wizard-backing-agent.md && grep -c '<capabilities>' skills/wizard-backing-agent.md && grep -c 'by_stage.planning' skills/wizard-backing-agent.md && grep -c 'toolkit-registry' skills/wizard-backing-agent.md | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
wizard-backing-agent.md contains Step 2.5 with bridge capability block injection before Task(bmad-gsd-orchestrator). Only by_stage.planning is read. toolkit-registry.json is not referenced. Route C has no injection.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these checks:

1. **INJ-02 (BMAD injection):** `grep -q '<capabilities>' skills/wizard-backing-agent.md && echo "INJ-02 PASS"`
2. **INJ-04 (no Skill injection):** Verify no `<capabilities>` reference near Skill() invocations:
   `grep -B2 -A2 "Skill(" skills/wizard.md | grep -c "capabilities"` should return 0
3. **CONF-01 (known-safe auto-inject):** `grep -q 'KNOWN_SAFE' skills/wizard.md && echo "CONF-01 PASS"`
4. **CONF-02 (batched confirmation):** `grep -q 'TOOLS_CONFIRMED' skills/wizard.md && echo "CONF-02 PASS"`
5. **CONF-03 (MCP conditional language):** `grep -q 'configured -- availability may vary' skills/wizard.md && echo "CONF-03 PASS"`
6. **PERF-03 (no registry load):** `grep -c 'toolkit-registry' skills/wizard.md skills/wizard-backing-agent.md` should return 0 matches (except in the rule forbidding it)
</verification>

<success_criteria>
- wizard.md has Step 2.5 with KNOWN_SAFE allowlist and AskUserQuestion confirmation flow
- All Agent() and Task() spawn points in wizard.md reference capability block construction
- wizard-backing-agent.md has Step 2.5 with planning-stage capability injection before bridge Task()
- No Skill() invocations in either file reference capability injection
- Neither file references toolkit-registry.json as a data source
- MCP tools use "configured -- availability may vary" suffix
</success_criteria>

<output>
After completion, create `.planning/phases/14-subagent-injection-confirmation-ux/14-01-SUMMARY.md`
</output>
